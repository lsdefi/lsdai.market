import React from 'react';

import RateHistory from './RateHistory';

const Hero = () => (
  <div className="hero">
    <div className="container relative">
      <RateHistory />
    </div>
  </div>
);

export default Hero;
